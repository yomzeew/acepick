import { View } from "react-native"
import LoginPage from "@pages/auth/login"

const LoginScreen = () => {
    return (
        <View style={{ flex: 1 }}>
            <LoginPage />
        </View>
    )
}
export default LoginScreen